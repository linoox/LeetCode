/**
 * Definition for binary tree
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */

// TODO
public class Solution {
    public int minDepth(TreeNode root) {
        
        if(root.left != null || depth <= minDepth) {
            traverser = root.left;
        } else {
            
        }
        
        
    }
}